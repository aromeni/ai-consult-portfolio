# Build 4 — AI Staff Training and Workshop Generator
# No external API keys are required for this prototype.
# All generation is deterministic and runs locally.

# Reserved for future phases only (e.g. optional Ollama integration):
# OLLAMA_BASE_URL=http://localhost:11434
